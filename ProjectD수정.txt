애니메이션 -> 공굴리기(push), 버튼터치, 손잡이 잡기

각 스테이지 시작시 -> UI로 스테이지 간단 설명

bgm 넣기

숲 ->

눈지형 -> 대사

절벽지형 -> 돌에서 움직일때 애니메이션 오류 생김(ground 판정이 아닌듯함), 가운데 벽 rendering mode 변경하기

동굴지형 -> 대사, 음악 넣기, 버튼입력시 음악재생, 버튼 material -> material property block 으로 바꾸기, 퀴즈 3개로 변경

캐릭터 -> (점프시 공중에 떠있는 시간), 시작 위치 고정, 공중상태여도 점프 한번가능하게-> 돌에서 내려올시 잠깐 공중판정으로 isjumpable false 가 됨 그지점찾기

카메라 -> 벽에 닿을시 카메라 넘어가지 않도록

UI

NPC 오브젝트
퀘스트

점프 -> 수업자료에서 점프 방식 확인(JumpAnim메서드를 pleyrAnim이 아닌 PlayerMovement에서 실행하는 것이 맞는가), 절벽에서 밀려나는이유

stage ->  끝날때 실행할 메서드를 작성하기 처음 대화 할때만 끝 이벤트 호출하기

처음시작시 설명 다시보고 싶으면 표지판 찾으라고 말하기

각 스테이지 설명 또 보고 싶으면 옆 표지판을 상호작용해서 보게 하기

모바일 말고 컴퓨터로 진행하는것 먼저하기
InputManager 구조 파악후 마우스 커버 표시 on,off 구현하기(대화 할때만 켜기)

* 싱글톤패턴, 대리자, 이벤트, Action, 코루틴
* InteractableNPC 에서
_TMP.rectTransform.eulerAngles = _PlayerCharacter.transform.position.Direction(transform.position).ToAngle() * Vector3.up;
* InteractableNPC, NPCdialog 구조 공부

저장하는 법

인게임 말고 씬에서만 작동하는 코드 있나 확인하기

Project Setting 에서 Static Batching 찾아보기(최적화), Stats에 관한것도 찾아보기

SoundManager, 표지판 작업(InteractableSign) 중

기획파일 push 하기